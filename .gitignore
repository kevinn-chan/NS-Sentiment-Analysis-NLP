# Secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environment
.venv/

# IDE
.idea/
.vscode/

# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Claude Code
.claude/

# Scratch / legacy
anaconda_projects/
logs/

# ── Data: raw ZST dumps (1–1.2 GB each) ──────────────────────────────────────
data/raw/

# ── Data: interim parquets (cleaned but pre-chunked, up to 2 GB) ─────────────
data/interim/

# ── Data: old/superseded outputs ──────────────────────────────────────────────
data/processed/old/
models/old/

# ── Large processed files (>100 MB GitHub limit) ─────────────────────────────
# These must be regenerated from the pipeline or downloaded separately.
# See README § "Large files not in this repository" for details.
data/processed/new/comments_chunks.parquet
data/processed/new/submissions_chunks.parquet
data/processed/new/chunk_faiss.index

# ── Model weights (1.2 GB SingBERT safetensors) ──────────────────────────────
# Config/tokenizer files ARE tracked; download weights from HuggingFace or retrain.
models/new/singbert_v7/model.safetensors
models/new/singbert_v7/training_args.bin

# BERTopic save artifact (duplicate subdir)
models/new/bertopic_fine/bertopic_fine/
